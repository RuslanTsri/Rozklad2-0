<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary: #4f46e5;
            --primary-light: #818cf8;
            --primary-dark: #3730a3;
            --surface: #ffffff;
            --text: #1e293b;
            --text-light: #64748b;
            --border: #e2e8f0;
            --gradient-start: #f8faff;
            --gradient-end: #eef2ff;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        }

        body {
            background: linear-gradient(135deg, var(--gradient-start), var(--gradient-end));
            color: var(--text);
            min-height: 100vh;
            line-height: 1.6;
        }

        .header {
            background: rgba(255, 255, 255, 0.95);
            box-shadow: 0 4px 20px rgba(79, 70, 229, 0.08);
            position: sticky;
            top: 0;
            z-index: 100;
            backdrop-filter: blur(12px);
            border-bottom: 1px solid rgba(79, 70, 229, 0.1);
        }

        .container {
            max-width: 1280px;
            margin: 0 auto;
            padding: 0 1.5rem;
        }

        .nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            height: 4.5rem;
        }

        .nav-brand {
            font-size: 1.25rem;
            font-weight: 700;
            color: var(--primary);
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            transition: all 0.3s ease;
        }

        .nav-brand:hover {
            color: var(--primary-dark);
            transform: translateY(-1px);
        }

        .nav-links {
            display: flex;
            gap: 1.25rem;
        }

        .nav-link {
            color: var(--text-light);
            text-decoration: none;
            font-weight: 500;
            padding: 0.625rem 1.25rem;
            border-radius: 0.75rem;
            transition: all 0.2s ease;
            border: 1px solid transparent;
        }

        .nav-link:hover {
            color: var(--primary);
            background-color: rgba(79, 70, 229, 0.08);
            border-color: rgba(79, 70, 229, 0.15);
        }

        .page-title {
            text-align: center;
            margin: 4rem 0;
            font-size: 2.75rem;
            font-weight: 800;
            background: linear-gradient(135deg, var(--primary), var(--primary-light));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: 0 2px 10px rgba(79, 70, 229, 0.15);
            letter-spacing: -0.02em;
        }

        .schedule-info {
            background: linear-gradient(135deg, var(--primary), var(--primary-light));
            color: white;
            padding: 1.75rem;
            border-radius: 1.25rem;
            margin-bottom: 2.5rem;
            text-align: center;
            box-shadow: 0 8px 30px rgba(79, 70, 229, 0.2);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 1rem;
            font-weight: 500;
            letter-spacing: 0.01em;
        }

        .schedule-container {
            background: white;
            border-radius: 1.5rem;
            box-shadow: 0 10px 40px rgba(79, 70, 229, 0.08);
            overflow: hidden;
            padding: 2rem;
            border: 1px solid rgba(79, 70, 229, 0.1);
        }

        .schedule {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0 1rem;
        }

        .schedule th {
            background: rgba(79, 70, 229, 0.04);
            color: var(--primary-dark);
            padding: 1.25rem 1rem;
            font-weight: 600;
            border-radius: 1rem;
            text-transform: uppercase;
            font-size: 0.875rem;
            letter-spacing: 0.05em;
            transition: all 0.2s ease;
        }

        .schedule th:hover {
            background: rgba(79, 70, 229, 0.08);
        }

        .time-slot {
            background: linear-gradient(135deg, var(--gradient-start), var(--gradient-end));
            text-align: center;
            width: 130px;
            border-radius: 1rem;
            padding: 1.25rem;
            transition: all 0.2s ease;
        }

        .time-slot:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(79, 70, 229, 0.12);
        }

        .time-number {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: var(--primary);
            color: white;
            width: 2.75rem;
            height: 2.75rem;
            border-radius: 1rem;
            margin-bottom: 0.75rem;
            font-weight: 700;
            box-shadow: 0 4px 12px rgba(79, 70, 229, 0.2);
            font-size: 1.125rem;
        }

        .lesson {
            background: linear-gradient(135deg, var(--gradient-start), var(--gradient-end));
            border-radius: 1.25rem;
            padding: 1.5rem;
            transition: all 0.3s ease;
            border: 1px solid rgba(79, 70, 229, 0.1);
            height: 100%;
        }

        .lesson:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 24px rgba(79, 70, 229, 0.12);
            border-color: var(--primary-light);
        }

        .lesson-subject {
            font-weight: 700;
            color: var(--primary-dark);
            margin-bottom: 1.25rem;
            font-size: 1.25rem;
            line-height: 1.4;
        }

        .lesson-type,
        .lesson-room,
        .lesson-teacher {
            display: flex;
            align-items: center;
            gap: 0.875rem;
            margin-bottom: 1rem;
            color: var(--text-light);
            font-size: 0.9375rem;
            transition: all 0.2s ease;
        }

        .lesson-type:hover,
        .lesson-room:hover,
        .lesson-teacher:hover {
            color: var(--primary);
        }

        .lesson-groups {
            display: flex;
            flex-wrap: wrap;
            gap: 0.625rem;
            margin-top: 1.25rem;
            padding-top: 1.25rem;
            border-top: 1px solid rgba(79, 70, 229, 0.1);
        }

        .group-tag {
            background: white;
            padding: 0.5rem 1rem;
            border-radius: 2rem;
            font-size: 0.8125rem;
            color: var(--primary);
            font-weight: 600;
            box-shadow: 0 2px 8px rgba(79, 70, 229, 0.08);
            border: 1px solid rgba(79, 70, 229, 0.1);
            transition: all 0.2s ease;
        }

        .group-tag:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(79, 70, 229, 0.15);
            background: var(--primary);
            color: white;
            cursor: pointer;
        }

        .footer {
            margin-top: 4rem;
            padding: 2.5rem 0;
            text-align: center;
            color: var(--text-light);
            font-size: 0.9375rem;
            border-top: 1px solid rgba(79, 70, 229, 0.1);
        }

        @media (max-width: 768px) {
            .container {
                padding: 1rem;
            }

            .page-title {
                font-size: 2rem;
                margin: 2.5rem 0;
            }

            .schedule-container {
                padding: 1rem;
                overflow-x: auto;
                -webkit-overflow-scrolling: touch;
            }

            .nav {
                flex-direction: column;
                height: auto;
                padding: 1.25rem 0;
                gap: 1.25rem;
            }

            .time-slot {
                width: 110px;
            }

            .lesson {
                padding: 1.25rem;
            }

            .schedule th {
                white-space: nowrap;
                padding: 1rem 0.75rem;
            }
        }

        .lesson-actions {
            display: flex;
            gap: 0.5rem;
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 1px solid rgba(79, 70, 229, 0.1);
        }

        .action-button {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 2.5rem;
            height: 2.5rem;
            border-radius: 0.75rem;
            border: 1px solid rgba(79, 70, 229, 0.1);
            background: white;
            color: var(--text-light);
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .edit-button:hover {
            background: var(--primary);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(79, 70, 229, 0.15);
        }

        .delete-button:hover {
            background: #ef4444;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(239, 68, 68, 0.15);
        }

        .time-slots-info {
            margin-top: 1rem;
            font-size: 0.875rem;
            color: var(--text-light);
        }
    </style>
</head>
<body>
<%- include('header') %>
<main class="container">
    <h1 class="page-title">Розклад групи <%= schedule.groupName %></h1>
    <div class="schedule-info">
        <i class="fas fa-info-circle fa-lg"></i>
        <span>Опубліковано розклад на <%= schedule.date %>. Слідкуйте за змінами, розклад коректується</span>
    </div>
    <div class="schedule-container">
        <table class="schedule">
            <thead>
            <tr>
                <th>Час</th>
                <th>Понеділок</th>
                <th>Вівторок</th>
                <th>Середа</th>
                <th>Четвер</th>
                <th>П'ятниця</th>
                <th>Субота</th>
                <th>Неділя</th>
            </tr>
            </thead>
            <tbody>
            <% const timeSlots = [
                "8:30 - 9:50",
                "10:00 - 11:20",
                "11:40 - 13:00",
                "13:30 - 14:50",
                "15:00 - 16:20",
                "16:30 - 17:50",
                "18:00 - 19:20"
            ] %>
            <% timeSlots.forEach((time, index) => { %>
                <tr>
                    <td class="time-slot">
                        <div class="time-number"><%= index + 1 %></div>
                        <div class="time-range"><%= time %></div>
                    </td>
                    <% for (let i = 0; i < 7; i++) { %>
                        <td>
                            <% if (slot?.lessons?.[i]) { %>
                                <div class="lesson">
                                    <div class="lesson-subject"><%= slot.lessons[i].subject %></div>
                                    <div class="lesson-type">
                                        <i class="fas fa-chalkboard-teacher"></i>
                                        <%= slot.lessons[i].type %>
                                    </div>
                                    <div class="lesson-room">
                                        <i class="fas fa-door-open"></i>
                                        <%= slot.lessons[i].room %>
                                    </div>
                                    <div class="lesson-teacher">
                                        <i class="fas fa-user"></i>
                                        <%= slot.lessons[i].teacher %>
                                    </div>
                                    <div class="lesson-groups">
                                        <% slot.lessons[i].groups.forEach(group => { %>
                                            <span class="group-tag"><%= group %></span>
                                        <% }) %>
                                    </div>
                                    <div class="lesson-actions">
                                        <button class="action-button edit-button" title="Редагувати">
                                            <i class="fas fa-pen"></i>
                                        </button>
                                        <button class="action-button delete-button" title="Видалити">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                </div>
                            <% } %>
                        </td>
                    <% } %>
                </tr>
            <% }) %>
            </tbody>
        </table>
    </div>
    <div class="time-slots-info">
        <p>Розклад дзвінків:</p>
        <% timeSlots.forEach((time, index) => { %>
            <p><%= index + 1 %> пара: <%= time %></p>
        <% }) %>
    </div>
</main>
<footer class="footer">
    <div class="container">
        <p>© 2025 Digital University UA</p>
    </div>
</footer>
</body>
</html>